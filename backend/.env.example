# Email Configuration for Feedback Submissions
# Configure these environment variables to enable email sending for beta feedback

# SMTP server settings (example: Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Email credentials
# For Gmail, use an App Password: https://support.google.com/accounts/answer/185833
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Feedback destination email
FEEDBACK_EMAIL=hi@andrea-antal.com

# Note: If these are not configured, feedback will be printed to the console instead
