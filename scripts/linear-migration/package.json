{
  "name": "linear-migration",
  "version": "1.0.0",
  "description": "Migrate Meal Planner project history into Linear",
  "type": "module",
  "scripts": {
    "migrate": "tsx migrate.ts",
    "migrate:dry-run": "tsx migrate.ts --dry-run"
  },
  "dependencies": {
    "@linear/sdk": "^29.0.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
